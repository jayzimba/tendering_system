import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PendingTenders = ({ navigation }) => {
  return (
    <View>
      <Text>PendingTenders</Text>
    </View>
  );
};

export default PendingTenders;

const styles = StyleSheet.create({});
